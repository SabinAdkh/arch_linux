require'nvim-tree'.setup {
    auto_close = true,
    diagnostics = {
        enable = false,
    },
    view = {
        width = 30,
        height = 30,
        side = 'left',
        auto_resize = false,
    },
}

